# Backend API port
PORT=3000

# ============================================================================
# SECURITY SETTINGS - REQUIRED FOR STARTUP
# ============================================================================

# JWT secret for signing authentication tokens
# REQUIRED: Must be at least 32 characters long
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Secret for encrypting sensitive data (GitHub tokens, etc.)
# REQUIRED: Must be at least 32 characters long
# Generate with: openssl rand -base64 32
ENCRYPTION_SECRET=

# CORS origin for the frontend
# REQUIRED: Must be the exact URL of your frontend (NO wildcards allowed)
# Example: https://yourdomain.com or http://localhost:3001
CORS_ORIGIN=http://localhost:3001

# ============================================================================
# DEFAULT ADMIN CREDENTIALS
# ============================================================================

# Default admin username
ADMIN_USERNAME=admin

# Default admin password
# IMPORTANT: Change this immediately in production!
# Generate strong password with: openssl rand -base64 24
ADMIN_PASSWORD=changeme

# ============================================================================
# DOCKER SECURITY WHITELISTS
# ============================================================================

# Allowed volume mount paths (comma-separated)
# Only these paths can be mounted into containers
# Default: Very restrictive. Add paths as needed.
# Example: /var/lib/docker/volumes,/opt/app-data,/home/*/data
ALLOWED_VOLUME_PATHS=/var/lib/docker/volumes,/opt/app-data,/tmp/docker-data

# Allowed Docker image registries (comma-separated)
# Only images from these registries can be pulled
# Empty string = Docker Hub (docker.io)
# Example: docker.io,gcr.io,ghcr.io
ALLOWED_REGISTRIES=docker.io,

# Allowed Git repository domains for image builds (comma-separated)
# Only repositories from these domains can be used for builds
# Example: github.com,gitlab.com
ALLOWED_GIT_DOMAINS=github.com

# ============================================================================
# DOCKER CONTAINER SETTINGS
# ============================================================================

# User and group IDs for the Docker container
# Get your UID: id -u
# Get your GID: id -g
# Get Docker GID: getent group docker | cut -d: -f3
UID=1000
GID=1000
DOCKER_GID=999
